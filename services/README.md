# Special services for rpc and log tools

* RabbitMQ
* Filebeat
* Elasticsearch
* Kibana

### Setup

```bash
$ docker-compose up kibana
$ docker-compose run filebeat setup
$ docker-compose up
```

For background work - `docker-compose up -d`
